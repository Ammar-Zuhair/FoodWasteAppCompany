# 🎨 إعداد أيقونة التطبيق

## الطريقة السريعة (موصى بها):

### 1. تشغيل الملف التلقائي:
- انقر نقراً مزدوجاً على ملف: **`إعداد_الأيقونة.bat`**
- سيتم نسخ الصورة تلقائياً

### 2. إنشاء الأيقونات:
افتح Terminal في مجلد `App_android` وقم بتشغيل:
```bash
npm run generate:icons
```

هذا الأمر سيقوم بـ:
- نسخ الصورة من `../image/logo.png` إلى `resources/`
- إنشاء جميع أحجام الأيقونات المطلوبة
- تحديث ملفات Android تلقائياً

### 3. مزامنة Capacitor:
```bash
npm run cap:sync
```

---

## الطريقة اليدوية:

إذا لم تعمل الأداة التلقائية:

### الخطوة 1: نسخ الصورة
1. افتح مجلد `App_android`
2. أنشئ مجلد `resources` إذا لم يكن موجوداً
3. انسخ `logo.png` من مجلد `image` إلى:
   - `App_android/resources/icon.png`
   - `App_android/resources/splash.png`

### الخطوة 2: إنشاء الأيقونات

**الخيار أ: استخدام أداة Capacitor Assets**
```bash
npx @capacitor/assets generate
```

**الخيار ب: استخدام موقع online**
1. افتح: https://www.appicon.co/
2. ارفع `logo.png`
3. حمّل الأيقونات بأحجام مختلفة
4. انسخها إلى:
   - `android/app/src/main/res/mipmap-mdpi/ic_launcher.png` (48x48)
   - `android/app/src/main/res/mipmap-hdpi/ic_launcher.png` (72x72)
   - `android/app/src/main/res/mipmap-xhdpi/ic_launcher.png` (96x96)
   - `android/app/src/main/res/mipmap-xxhdpi/ic_launcher.png` (144x144)
   - `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher.png` (192x192)

### الخطوة 3: مزامنة
```bash
npm run cap:sync
```

---

## ملاحظات مهمة:

- ✅ الصورة يجب أن تكون PNG
- ✅ الحجم الموصى به: 1024x1024 بكسل
- ✅ بعد إنشاء الأيقونات، سيتم تحديث ملفات Android تلقائياً
- ✅ يمكنك رؤية الأيقونة الجديدة بعد إعادة بناء APK

---

## التحقق من النتيجة:

بعد إعداد الأيقونة:
1. افتح Android Studio
2. ابني APK جديد
3. ثبّت التطبيق على الجوال
4. ستظهر الأيقونة الجديدة على الشاشة الرئيسية









