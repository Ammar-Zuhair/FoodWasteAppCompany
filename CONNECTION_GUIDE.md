# 🔌 دليل الاتصال بالباك اند

## ✨ التحسينات الجديدة

التطبيق الآن يحاول الاتصال بالباك اند تلقائياً عند:
- ✅ فتح صفحة تسجيل الدخول
- ✅ محاولة تسجيل الدخول
- ✅ تغيير الشبكة

---

## 🚀 كيف يعمل

### 1. عند فتح صفحة تسجيل الدخول:
- التطبيق يبحث تلقائياً عن IP صحيح
- يجرب IP المحفوظ أولاً
- إذا فشل، يبحث في قائمة IPs محتملة
- يحدث IP تلقائياً

### 2. عند محاولة تسجيل الدخول:
- التطبيق يتحقق من IP قبل المحاولة
- إذا فشل، يبحث عن IP جديد تلقائياً
- يحاول تسجيل الدخول مرة أخرى

### 3. عند تغيير الشبكة:
- التطبيق يراقب تغييرات الشبكة
- يكتشف IP جديد تلقائياً
- يحدث API_CONFIG تلقائياً

---

## 📱 خطوات الاتصال

### الخطوة 1: تأكد من أن الباك اند يعمل

```bash
cd backend
python start_server.py
```

يجب أن ترى:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### الخطوة 2: افتح التطبيق
- التطبيق سيبحث تلقائياً عن IP صحيح
- انتظر قليلاً (2-3 ثواني)

### الخطوة 3: إذا لم يتصل تلقائياً
1. اضغط "إعدادات الاتصال" في صفحة تسجيل الدخول
2. اضغط "البحث التلقائي عن IP"
3. أو أدخل IP يدوياً

---

## 🔧 استكشاف الأخطاء

### المشكلة: لا يتصل تلقائياً

**الحلول:**

1. **تأكد من أن الباك اند يعمل:**
   ```bash
   # في terminal منفصل
   cd backend
   python start_server.py
   ```

2. **تحقق من IP الكمبيوتر:**
   ```powershell
   # في PowerShell
   ipconfig
   ```
   ابحث عن "IPv4 Address" تحت Wi-Fi أو Ethernet

3. **استخدم البحث التلقائي:**
   - في صفحة تسجيل الدخول
   - اضغط "إعدادات الاتصال"
   - اضغط "البحث التلقائي عن IP"

4. **تحقق من Firewall:**
   ```powershell
   # شغّل كـ Administrator
   .\CHECK_IP_AND_FIREWALL.ps1
   ```

### المشكلة: IP يتغير كثيراً

**الحل:**
- استخدم Hotspot من الجوال (أكثر استقراراً)
- أو عيّن IP ثابت في إعدادات Windows

---

## 💡 نصائح

1. **استخدم Hotspot من الجوال:**
   - أكثر استقراراً
   - IP لا يتغير
   - أسهل للاتصال

2. **تأكد من نفس الشبكة:**
   - الجوال والكمبيوتر يجب أن يكونا على نفس الشبكة
   - Wi-Fi أو Hotspot

3. **البحث التلقائي:**
   - أسهل طريقة
   - جربها أولاً قبل الإعدادات اليدوية

---

## 🎯 الملفات المعدلة

1. `src/pages/LoginPage.jsx` - اكتشاف تلقائي عند فتح الصفحة
2. `src/utils/api/auth.js` - اكتشاف تلقائي قبل تسجيل الدخول
3. `src/config/api.config.js` - تحسين سرعة الاختبار
4. `src/utils/networkIPDiscovery.js` - تحسين آلية الاكتشاف

---

## ⚡ التحسينات التقنية

### 1. اختبار أسرع
- Timeout: 3 ثواني → 2 ثانية
- اختبار متوازي لجميع IPs

### 2. اكتشاف تلقائي
- عند فتح صفحة تسجيل الدخول
- قبل محاولة تسجيل الدخول
- عند تغيير الشبكة

### 3. تحديث تلقائي
- API_CONFIG يتم تحديثه تلقائياً
- لا حاجة لإعادة تحميل في معظم الحالات

---

**تاريخ التحديث:** 2025-12-20






