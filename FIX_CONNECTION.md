# 🔧 إصلاح مشكلة الاتصال بالخادم

## المشكلة:
التطبيق لا يستطيع الاتصال بالخادم (Backend) على الجوال.

## الحل:

### 1. معرفة IP الكمبيوتر:

**على Windows:**
```bash
ipconfig
```
ابحث عن `IPv4 Address` تحت `Wireless LAN adapter Wi-Fi` أو `Ethernet adapter`

مثال: `192.168.1.105`

**على Mac/Linux:**
```bash
ifconfig
# أو
ip addr
```

### 2. تعديل IP في التطبيق:

#### الطريقة الأولى: استخدام متغير البيئة (موصى بها)

أنشئ ملف `.env.local` في مجلد `App_android`:

```env
VITE_API_URL=http://YOUR_IP:8000
VITE_LLAMA_URL=http://YOUR_IP:8001
```

مثال:
```env
VITE_API_URL=http://192.168.1.105:8000
VITE_LLAMA_URL=http://192.168.1.105:8001
```

ثم أعد بناء التطبيق:
```bash
npm run build
npm run cap:sync
```

#### الطريقة الثانية: تعديل الكود مباشرة

افتح `App_android/src/config/api.config.js` وعدّل:

```javascript
const possibleIPs = [
  'YOUR_IP_HERE',  // ضع IP الكمبيوتر هنا
  '192.168.1.100',
  // ...
];
```

#### الطريقة الثالثة: من Console المتصفح (للاختبار)

افتح التطبيق في المتصفح وافتح Console (F12)، ثم:

```javascript
localStorage.setItem('backend_ip', 'YOUR_IP_HERE');
location.reload();
```

### 3. التأكد من أن Backend يعمل:

```bash
# في مجلد backend
python start_server.py
```

يجب أن ترى:
```
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### 4. التأكد من Firewall:

- تأكد من أن Windows Firewall يسمح بالاتصالات على المنفذ 8000
- أو قم بإيقاف Firewall مؤقتاً للاختبار

### 5. التأكد من أن الكمبيوتر والجوال على نفس الشبكة:

- يجب أن يكونا على نفس Wi-Fi
- لا تستخدم VPN

## اختبار الاتصال:

افتح المتصفح على الجوال واذهب إلى:
```
http://YOUR_IP:8000/api/v1/health
```

يجب أن ترى:
```json
{"status":"ok"}
```

## بعد التعديل:

1. أعد بناء التطبيق:
   ```bash
   npm run build
   npm run cap:sync
   ```

2. افتح Android Studio:
   ```bash
   npm run cap:open:android
   ```

3. ابني APK جديد

4. ثبّت التطبيق على الجوال

## ملاحظات:

- IP قد يتغير عند إعادة الاتصال بالشبكة
- استخدم IP ثابت (Static IP) إذا أمكن
- يمكنك استخدام `ipconfig /all` لمعرفة IP بشكل دائم









