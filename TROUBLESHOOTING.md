# 🔧 حل مشاكل الاتصال من الجوال

## المشكلة: "لا يمكن الاتصال بالخادم"

### الحلول السريعة:

#### 1. ✅ تأكد من أن Backend يعمل على `0.0.0.0:8000`

```powershell
cd F:\Reducing_Food_Waste_HSA\backend
& "C:/Users/PC/AppData/Local/Programs/Python/Python313/python.exe" -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

**مهم جداً:** استخدم `0.0.0.0` وليس `localhost`!

#### 2. ✅ تأكد من أن الجوال والكمبيوتر على نفس شبكة Wi-Fi

- افتح إعدادات Wi-Fi على الجوال
- تأكد من أنك متصل بنفس الشبكة التي يتصل بها الكمبيوتر

#### 3. ✅ اختبر الاتصال من متصفح الجوال

افتح متصفح الجوال واذهب إلى:
```
http://192.168.8.184:8000/docs
```

إذا ظهرت صفحة Swagger UI، يعني Backend يعمل ✅

إذا لم تظهر، المشكلة في الشبكة أو Firewall.

#### 4. ✅ افتح Firewall للمنفذ 8000

```powershell
New-NetFirewallRule -DisplayName "Python Backend Port 8000" -Direction Inbound -LocalPort 8000 -Protocol TCP -Action Allow
```

#### 5. ✅ تحقق من IP الحالي

```powershell
Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -like "192.168.*" -and $_.InterfaceAlias -notlike "*VMware*" }
```

إذا تغير IP، حدثه في `App_android/src/config/api.config.js` وأعد البناء.

#### 6. ✅ أعد بناء التطبيق

```bash
cd F:\Reducing_Food_Waste_HSA\App_android
npm run build
npm run cap:sync
```

ثم ابني APK جديد في Android Studio.

### حلول متقدمة:

#### تغيير IP من داخل التطبيق (إذا كان التطبيق يدعم localStorage):

افتح Console في التطبيق (إذا كان متاحاً) واكتب:
```javascript
localStorage.setItem('backend_ip', '192.168.8.184');
```

ثم أعد فتح التطبيق.

#### استخدام IP ثابت:

1. في إعدادات Wi-Fi على الكمبيوتر، اضبط IP ثابت
2. أو استخدم Hotspot من الجوال واتصل الكمبيوتر به

### اختبار شامل:

1. ✅ Backend يعمل على `0.0.0.0:8000`
2. ✅ يمكن الوصول من متصفح الجوال: `http://192.168.8.184:8000/docs`
3. ✅ Firewall مفتوح للمنفذ 8000
4. ✅ الجوال والكمبيوتر على نفس الشبكة
5. ✅ IP محدث في `api.config.js`
6. ✅ التطبيق مبني حديثاً

إذا استمرت المشكلة بعد كل هذا، قد تكون المشكلة في:
- إعدادات Router (قد يمنع الاتصال بين الأجهزة)
- VPN أو Proxy على الجوال
- إعدادات الأمان على الجوال








